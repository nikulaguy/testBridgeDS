---
import { Icon } from "astro-icon/components";
import { siteName } from "../site.config";
---

<Icon
  name='frontguys-logo'
  title={siteName}
  role='img'
  class='a-logo'
  width={138}
  height={40}
  style={{ width: `var(--logo-width)` }}
/>
